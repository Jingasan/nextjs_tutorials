/**
 * app/loading-test/loading.tsx
 * ローディング
 *
 * ローディングは、時間のかかるコンポーネントを表示する際に非同期で呼び出しを行い、
 * 待っている間ローディングアイコン等を表示することができる。
 * 以下は、ローディングアイコン等を表示するコンポーネントである。
 */
export default function Loading() {
  return <div>Loading...</div>
}
