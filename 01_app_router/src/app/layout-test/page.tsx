/**
 * app/page-test/page.tsx
 * Pageファイル
 *
 * Pageファイルは、実際にユーザーがアクセスする個別のページを定義するために使用される。
 * 特定のセグメントに対応するPageファイルを配置することで、特定Routeの画面を提供する。
 */
export default function Page() {
  return <h1>Layout Test</h1>;
}
